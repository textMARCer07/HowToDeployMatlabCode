function input_value = TestModelSingleInOutVariable(input_value)
    % This function takes an input variable, multiplies it by 12 and puts
    % the result in the same variable

    input_value = input_value * 12;
end
